the project URL of this solution can be found at [here](https://roadmap.sh/projects/broadcast-server). 

# Usage

To start the server: `broadcast-server start`, press `CTRL+C` to shutdown the server.

To start a client: `broadcast-server connect`.
