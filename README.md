The project URL of this solution can be found [here](https://roadmap.sh/projects/whisperly). 

# Usage

To start the server: `whisperly start`, press `CTRL+C` to shutdown the server.

To start a client: `whisperly connect`.
